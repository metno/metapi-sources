# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Sources
GET     /v0.:format                 controllers.SourcesController.getSources(ids: Option[String], types: Option[String], bbox: Option[String], validtime: Option[String], fields: Option[String], format: String)
